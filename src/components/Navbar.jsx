import React from 'react'
import App from '../App.css'

const Navbar = () => {
    return (
        <nav>
            <div class="logo">
                <a href="#">Заголовок</a>
            </div>
            <div class="login">
                <a href="#">login</a>
            </div>
        </nav>
    )
}

export default Navbar